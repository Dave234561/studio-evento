import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EventAI Pro - Intelligence Événementielle',
  description: 'Plateforme d\'intelligence artificielle pour la planification d\'événements professionnels',
  keywords: ['événements', 'IA', 'planification', 'RSE', 'storytelling'],
  authors: [{ name: 'Manus AI' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100">
          {children}
        </div>
      </body>
    </html>
  )
}